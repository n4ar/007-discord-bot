const { MessageEmbed, VoiceState } = require('discord.js')
const config = require('../../config')
module.exports = {
    name: "playlist",
    type: "music",
    description: "เปิดเพลงเเบบ playlist",
    aliases: ['playlist', 'playl', 'pl'],
    async run(client, message, args, language) {
        try {
            
        } catch (e) {
            console.log(e)
        }
    }
}